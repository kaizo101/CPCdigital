# The dormant v2 server prototype fails closed without a strong JWT secret.
# Generate a local value with: openssl rand -hex 32
JWT_SECRET=

HOST=127.0.0.1
PORT=3001
CLIENT_ORIGIN=http://localhost:5173
DB_PATH=./.local-data/cpcdigital.db
